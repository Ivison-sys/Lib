#include <bits/stdc++.h> 
#define nl << '\n' 
#define ll long long 
using namespace std; 

class Grafo {
public:



};

void buscaProfundidade(int v){
    bool marcado[v]; for(int i = 0; i < v; i++) marcado[i] = false;
    int antecessor[v]; for(int i = 0; i < v; i++) antecessor[i] = -1;
    for(int i = 0; i < v; i++){
        // if(!marcado[v]) dfs()
    }
}

int main(){ 
    ios::sync_with_stdio(false);
    cin.tie(0); 
    
    return 0; 
}